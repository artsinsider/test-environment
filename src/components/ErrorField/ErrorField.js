import React from 'react';function ErrorField (props) {    const {input, type, meta: {error, touched}} = props;    return (        <div className="error-field">            <div className="label-field">{input.name}</div>            <input className="input-field" {...input} type={type}/>            {touched && error && <div className="error-valid" >{error}</div>}        </div>    );}export default ErrorField