import React from 'react';function ErrorField (props) {    const {input, type, meta: {error, touched}, typeComponent} = props;    function ageNumber (){        const arrOptions = [];        for(let i = 18; i < 50; i++) {            const node = <option key={i} value={i}>{i}</option>;            arrOptions.push(node)        }        return arrOptions    };    if (typeComponent === 'select') {        return (            <div className="error-field">                <div className="label-field">{input.name}</div>                <select {...input} className="select-field" name={input.name} type={type}>                    {ageNumber()}                </select>            </div>        );    }    if (typeComponent === 'radio') {        return (            <div className="error-field">                <div className="label-field">{input.name}</div>                <div className="radio-field">                    {props.dataRadio.map((el, i) =>                        <span className="radio-item" key={i}>                            <span>{el}</span>                            <input {...input} type={type} name={input.name} value={el} />                        </span>                    )}                </div>            </div>        );    }    return (        <div className="error-field">            <div className="label-field">{input.name}</div>            <input className="input-field" {...input} type={type}/>            {touched && error && <div className="error-valid" >{error}</div>}        </div>    );}export default ErrorField