import {appName} from '../firebase'import {Record, List}  from 'immutable'import uid       from 'uuid/v4'export const moduleName  = 'people';export const ADD_PEOPLE_START = `${appName}/${moduleName}/ADD_PEOPLE_START`;export const ADD_PEOPLE_SUCCESS = `${appName}/${moduleName}/ADD_PEOPLE_SUCCESS`;export const ADD_PEOPLE_ERROR = `${appName}/${moduleName}/ADD_PEOPLE_ERROR`;const ReducerRecord = Record({    peoples: new List([]),    loading: false});const PoepleRecord = Record({    id: null,    name: null,    lastName: null,    age: null,    sex: null,    course: null});export default function peopleReducer(state = new ReducerRecord, action) {    const {type} = action;    switch (type) {        case ADD_PEOPLE_START:            return state.set('loading', true);        case ADD_PEOPLE_SUCCESS:            return state.update('peoples', peoples => peoples.push( new PoepleRecord(action.data)))                   .set('loading', false)        case ADD_PEOPLE_ERROR:            return state.set('error', action.error)                .set('loading', false);        default: return state;    }}export function addPeople(user) {    console.log('user', user)    return {        type: ADD_PEOPLE_SUCCESS,        data: {id: uid(), ...user}    }}